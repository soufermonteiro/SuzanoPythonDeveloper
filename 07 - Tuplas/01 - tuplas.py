# não é a mesma coisa que lista
# Tupla é imutável (não se altera os valores)
frutas = ("laranja", "pera", "uva",)
letras = tuple("python")
numeros = tuple([1, 2, 3, 4])
pais = ("Brasil",)

# para acessar é igual às listas
print(frutas[-1])

#tambem podemos fazer o fatiamento
print(frutas[::-1])
